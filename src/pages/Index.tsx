import { Layout } from "@/components/Layout";
import { LandingPage } from "@/pages/LandingPage";

const Index = () => {
  return (
    <Layout>
      <LandingPage />
    </Layout>
  );
};

export default Index;
